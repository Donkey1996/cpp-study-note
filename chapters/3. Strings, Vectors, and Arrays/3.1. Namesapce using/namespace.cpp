#include <iostream>

//for each name we use there should be a using declaration.
using std::cin;
using std::cout;
using std::endl;
int main()
{
    double input = 0.0;
    cin >> input;
    cout << "You entered: " << input << endl; 

    return 0;
}